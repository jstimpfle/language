proc main() int
{
    print64(7 + 2);
    print64(7 - 2);
    print64(7 * 2);
    print64(7 / 2);
    print64(3 & 2);
    print64(2 | 1);
    /* NOTE: the code emitted for this currently causes the program to crash on windows */
    /*print64(~1);*/
    return 0;
}

export main;
