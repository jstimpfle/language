proc int fib(int a)
{
    if (a > 1)
        return fib(a - 1) + fib(a - 2);
    else
        return 1;
}

proc int main()
{
    data int r;
    r = fib(7);
    print64(r);
    return 0;
}

export main;
