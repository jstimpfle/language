macro mul(x, y) = x * y;

proc main() int
{
    data x int;
    data y int;
    macro foo(a, b) = x + mul(a, b);
    x = 1;
    {
        data x int;
        x = 13;
    }
    y = 42;
    x = foo(3, 5);
    print64(x);
    return 0;
}

export main;
