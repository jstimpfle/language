proc main(argc int, argv ^^char) int
{
    if (argc < 2) {
        prints("Need at least 1 command-line argument!\n");
        return 1;
    }
    for i from 1 to argc do {
        prints("range arg: ");
        prints(argv[i]);
        prints("\n");
    }
    return 0;
}

export main;
