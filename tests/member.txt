struct foo {
    data int x;
    data int y;
    data int z;
}

proc int main()
{
    data foo a;

    a.x = 3;
    a.y = 4;
    a.z = 5;

    print64(a.x);
    print64(a.y);
    print64(a.z);

    print64((&a.x)[0]);
    print64((&a.x)[1]);
    print64((&a.x)[2]);

    return 0;
}

export main;
