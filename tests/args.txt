proc localproc() int
{
}

proc main(argc int, argv ^^char) int
{
    if (argc < 2) {
        prints("Need 1 command-line argument!\n");
        return 1;
    }
    prints("The command-line argument is: ");
    prints(argv[1]);
    prints("\n");
    return 0;
}

export main;
