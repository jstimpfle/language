data int test;
data bla bla;

proc int test(int x, int a)
{
    data foo y;
    *&z = 1;
    z = (1 + 3) + 3 * 4;
    y = 3 + 4;
    if (z++ + 2)
        a = b;
    if (z  ++  -- + 2)
        a = b;
    if (z + 2) {
        a = b;
    }
    testcall(3, y*y);
    testcall(3, y*(y+z));

    (z++)(0, 18);
}

data int lastvar;
