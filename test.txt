entity int Foobar;

table Foobar baz {
    column int bazx;
    column int bazy;
}

data int test;
data bla blub;
data int testcall;

proc int test(int x, int a)
{
    data int y;
    data foo z;
    *&z = 1;
    z = (1 + 3) + 3 * 4;
    y = 3 + k;
    if (z++ + 2)
        a = b;
    if (z  ++  -- + 2)
        a = b;
    if (z + 2) {
        a = b;
    }
    testcall(3, g(y*y), 5);
    testcall(3, y*(y+z));

    (z++)(0, 18);
}

proc int test2(int x, int a)
{
        return x * a;
}

data int lastvar;
