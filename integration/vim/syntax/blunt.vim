syntax keyword bluntkw enum struct data proc macro constant export extern if then else while for do from to downto in return break continue
syntax match bluntop "=>"
hi link bluntkw keyword
hi link bluntop keyword
